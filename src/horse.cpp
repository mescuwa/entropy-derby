#include "horse.hpp"

namespace it {
// The Horse struct is defined inline in the header; this translation unit
// exists to give the engine a dedicated compilation unit for potential future
// functionality (e.g., serialization helpers).
}

